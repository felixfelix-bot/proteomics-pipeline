---
# r-base role: installs R 4.x from CRAN on Ubuntu/Debian
# Entry point — delegates to debian.yml (Ubuntu only, Windows was removed)

- name: Install R on Debian/Ubuntu
  include_tasks: debian.yml
  when: ansible_os_family == "Debian"